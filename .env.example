# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/grapplemap

# Authentication
NEXTAUTH_SECRET=your-random-secret-key-here-generate-with-openssl
NEXTAUTH_URL=http://localhost:3000

# Stripe Payment Configuration (Required for paid tiers)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PRICE_STANDARD=price_...  # Standard tier price ID
STRIPE_PRICE_PRO=price_...       # Pro tier price ID
STRIPE_PRICE_ACADEMY=price_...   # Academy tier price ID
STRIPE_WEBHOOK_SECRET=whsec_...  # Webhook signing secret
STRIPE_PORTAL_URL=https://billing.stripe.com/p/login/...

# QR Code Security (Required for check-in feature)
QR_SECRET=your-qr-signing-secret-min-32-chars

# Email Configuration (Optional for launch, required for production)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=your-sendgrid-api-key
# SMTP_FROM=noreply@grapplemap.uk

# Application URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Analytics (Optional)
# NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id
